function one() {
	// transform from 16 Bit in String 
	var load = JSON.parse(data);

	// transform from obj in string 
	var data = JSON.stringify(words);
	if (typeof === 'string') {
		console.log();
	}

	console.log(load);
}